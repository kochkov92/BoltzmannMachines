apply plugin: 'cpp'
model {
  components {
    main(NativeExecutableSpec) {
      binaries.all {
        cppCompiler.args "-O2", "-std=c++11"
      }
      sources {
        cpp {
          source {
            srcDir "src/cc"
          }
        }
      }
    }
  }
}
