#include <vector> // storing
#include <utility> // pair<,>

namespace bMachine {
  using ind = unsigned int;
  using bVec = std::vector<bool>;
  using bond = std::pair<ind, ind>;
  using edges = std::vector<bond>;
  using dVec = std::vector<double>;
  using indVec = std::vector<ind>;
}